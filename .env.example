# The site is fully static and needs no env vars to run locally.

# Canonical site origin for metadata, sitemap, OG URLs, and JSON-LD.
# Defaults to https://andrewkaiserauer.com — override for previews/forks.
# NEXT_PUBLIC_APP_URL="http://localhost:3000"
